Yannotate needs your project to provide the following libraries:

Bower-fetchable:
- Bootstrap

For the sample app to work you need to bower link the yannotate library.

```
$ cd dist/
$ bower link
$ cd ../yannotate-app/
$ bower link dist
```
